(()=>{document.addEventListener('DOMContentLoaded',()=>document.querySelectorAll('[data-roadmap]').forEach(list=>{const branch=list.dataset.roadmap;const items=window.PROJECT_STATUS?.[branch]||[];list.innerHTML=items.map(item=>{const [label,status]=item.split('|');const cls=status==='confirmed'?'done':status==='progress'?'active':'future';const word=status==='confirmed'?'vorhanden':status==='progress'?'in Arbeit':'geplant';return `<li class="${cls}"><strong>${label}</strong><br><span class="muted">${word}</span></li>`}).join('')}))})();
